eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

spring:
  datasource:
    url: jdbc:postgresql://users_db:5432/who_users_db
    username: admin
    password: root


management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9411/api/v2/spans
